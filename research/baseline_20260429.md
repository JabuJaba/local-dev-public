# Baseline Numérico — 2026-04-29
**Sprint 10 Phase 1** | Setup: RTX 5070 Ti 16GB GDDR7 | Ryzen 7950X3D | 64GB DDR5 | Windows 11

## Ambiente

| Campo | Valor |
|-------|-------|
| CUDA Version | 13.2 |
| Driver NVIDIA | 595.79 |
| GPU | RTX 5070 Ti (sm_120 / Blackwell consumer) |
| VRAM total | 16303 MB |
| VRAM idle | ~1200–1310 MB |

**Nota CUDA 13.2:** Conforme handoff `research/llm-optimization-handoff.md`, CUDA 13.2 produz gibberish com Qwen3.6. Driver 595.79. Fix em andamento pela NVIDIA.

## Phase 2 — CUDA Check: Deferred

**Status:** Deferred (não N/A puro — CUDA 13.2 está ativo, mas downgrade foi adiado)

**Justificativa:**
- CUDA 13.2 / Driver 595.79 confirmados via `nvidia-smi`
- Downgrade de driver é operação destrutiva com risco a outras aplicações GPU; adiado
- **Sanity check 2026-04-29:** qwen3.6:35b-a3b-q4_k_m produziu output coerente (17.4 t/s, sem gibberish detectado). Bug documentado é mais severo no Qwen3.6-27B denso; 35B-A3B MoE aparenta ser menos afetado
- Fix aguardado via NVIDIA (watchlist item 5 em `CLAUDE.md`); ao mergear → atualizar driver e re-rodar `scripts/baseline_measure.py`

**Acceptance:** Fase marcada como "deferred" com versão atual registrada (CUDA 13.2) e saída coerente confirmada.

## Método de Medição

- **Endpoint:** Ollama `/api/generate` com `stream: false`
- **Prompt:** fixo (função Python de 187–208 tokens conforme tokenização por modelo)
- **num_predict:** 200 tokens
- **Parâmetros:** temperature=0.7, top_p=0.8, top_k=20, repeat_penalty=1.05
- **VRAM:** `nvidia-smi --query-gpu=memory.used --format=csv,noheader,nounits` antes/depois
- **Timing:** campos nativos Ollama: `prompt_eval_count/duration`, `eval_count/duration`
- **Entre modelos:** `ollama stop` para evitar VRAM contention (CLAUDE.md gotcha)
- **Script:** `scripts/baseline_measure.py`

**Comando reproduzível:**
```bash
PYTHONIOENCODING=utf-8 python scripts/baseline_measure.py
```

## Tabela de Métricas (12 valores)

| Modelo | CUDA Ver | Prefill t/s | TG t/s | VRAM MB |
|--------|----------|-------------|--------|---------|
| qwen3.6:35b-a3b-q4_k_m | 13.2 | 284.3 | 17.4 | 14441 |
| gemma4:26b | 13.2 | 842.4 | 31.5 | 14234 |
| qwen3coder-local (A3B MoE) | 13.2 | 457.6 | 37.3 | 14115 |

**Todos os 3 modelos usam ~14–15GB de VRAM** (dentro dos 16GB disponíveis com ~1.2–1.4GB idle).

## Notas por Modelo

### qwen3.6:35b-a3b-q4_k_m
- TG t/s (17.4) menor que qwen3coder-local (37.3): modelo maior + offload para RAM (~7GB extrapolado pelo tamanho do arquivo vs VRAM)
- Prefill t/s (284.3) mais lento que qwen3coder-local (457.6): mesma razão
- **NOTA CRÍTICA:** CUDA 13.2 pode degradar qualidade (gibberish). Sprint 10 Phase 2 deve endereçar.

### gemma4:26b
- Prefill t/s (842.4) > 2x qwen3coder-local — arquitetura mais amigável para batch prefill
- TG t/s (31.5) — intermediário entre os dois qwen
- VRAM (14234 MB) — cabendo quase totalmente na VRAM

### qwen3coder-local (qwen3-coder:30b-a3b-q4_K_M A3B MoE)
- Melhor TG t/s (37.3) dos 3 modelos
- Prefill t/s (457.6) — intermediário
- VRAM (14115 MB) — menor dos 3

## Flash Attention (Phase 3)

- **Ollama:** ativo por padrão em versões recentes (confirmado via comportamento medido — prefill gemma4 842 t/s indica FA ativo)
- **llama-server (Slot 3):** `--flash-attn on` em `start.ps1:46`; boot log confirma `llama_context: flash_attn = enabled`
- Benchmark 32K antes/depois não realizado: flag já estava ativa desde a configuração inicial

## NVFP4 / ComfyUI (Phase 4)

- **ComfyUI 0.3.44** instalado em `D:\ComfyUI_Executavel\`
- Modelos presentes: sd_xl_base_1.0.safetensors, sd_xl_refiner_1.0.safetensors
- **Flux.1 não instalado** — NVFP4 checkpoint específico para Flux.1 não aplicável
- **Phase 4 → N/A:** Justificativa: Flux.1 não está instalado nem em uso ativo
